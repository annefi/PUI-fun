#ifndef LIBETPH_MATH_HH
#define LIBETPH_MATH_HH
//-----------------------------------------------------------------------------
//! very general gauss functor
class Gauss {
public:
    Gauss(double _sigma, double _mu) : sigma(_sigma),
				       mu(_mu) {}
    /*
      Gauss(double _sigma, double _mu) {
      sigma=_sigma;
      mu=_mu;
      }
    */
	
    double operator()(double x) {
	FIXME
	    }

private:
    double sigma, mu;
};
//-----------------------------------------------------------------------------
#endif
