/* using strings */

#include <stl.h>
#include <string>
#include <stdio.h>

int main(){
  string base = "axlv2";
  string ext = "dat";

  long int s1 = 2000158;
  long int s2 = 2000165;

  char cstr1[16];
  sprintf(cstr1, "%d_%d", s1,s2);
  
  string s3 = base + "_" + cstr1 + "." + ext;

  // DNW
  //string s3 = base + "_" + (string) s1 + "_" + (string) s2 + "." + ext + "\n";
  cout << s3 << endl;
 
 
}
