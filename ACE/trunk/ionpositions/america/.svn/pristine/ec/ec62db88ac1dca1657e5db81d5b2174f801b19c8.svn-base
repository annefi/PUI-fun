// this is just a code fragment stored here for future possible use

  // test of matrix inversion
  const int N = 2;
  TNT::Matrix<float> A(N,N,
		       " 1 2"
		       "-1 1");

  TNT::Matrix<float> Ainv(N,N,0.0);

  Ainv = Invert(A);
  
  cout << thisprog << " -I- Ainv=" << endl;
  cout << Ainv;
