/* using strings */

#include <stl.h>
#include <string>
#include <stdio.h>

int main(){
  string base = "axlv2";
  string ext = "dat";

  long int s1 = 2000158;
  long int s2 = 2000165;

  char *step;
  string s3;

  for (int nedb = 2; nedb < 60; nedb++){
    sprintf(step, "%02d", nedb);
  
    s3 = base + "_" + step + "." + ext;

    cout << s3 << endl;
  }
 
 
}
