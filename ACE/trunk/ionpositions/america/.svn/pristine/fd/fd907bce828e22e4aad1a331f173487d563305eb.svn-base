#include "swindal.h"
#include <fstream>

int main(){
  char thisprog[] = "initvec";
  char slicefile[20];
  //int slices[] = {18, 24, 30, 36, 42, 48};
  vector<int> slices = {18, 24, 30, 36, 42, 48};
  for (int i = 0; i < 6; i++){
    
    sprintf(slicefile, "ma-sl%02d.%s",slices[i], thisprog);
    cout << thisprog << " -I- " << "slicefile=" << slicefile << endl;
  }

  return 0;
}
