The names of these two include files, nr.h and nrutil.h, have been changed
to avoid conflict with NR C++.  In order to built this NR C library, one must
symbolically link the new names, nr_c.h and nrutil_c.h, back to the originals,
since the NR C routines use the original names.

Jim Raines, 20Jul2005
